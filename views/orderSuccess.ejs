<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ZozoMart | Order placed</title>
  <style>
@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap');
:root {
  --bg: #fdf7fb;
  --text: #111827;
  --muted: #6b7280;
  --accent: #ff5c8a;
  --success: #22c55e;
  --border: 1px solid rgba(17,24,39,0.05);
  --shadow: 0 25px 45px rgba(17,24,39,0.1);
  --card: #fff;
}
* { box-sizing: border-box; }
body {
  margin: 0;
  font-family: 'Poppins', sans-serif;
  background: radial-gradient(circle at 15% 20%, rgba(255, 92, 138, 0.15), transparent 45%), linear-gradient(180deg, #fff, var(--bg));
  color: var(--text);
}
.container {
  max-width: 1100px;
  margin: 0 auto;
  padding: 24px;
}
.nav {
  display: flex;
  align-items: center;
  justify-content: space-between;
  border-radius: 999px;
  border: var(--border);
  background: rgba(255,255,255,0.95);
  padding: 12px 18px;
  box-shadow: 0 12px 30px rgba(17,24,39,0.09);
  position: sticky;
  top: 12px;
  z-index: 10;
}
.brand { display: flex; align-items: center; gap: 10px; font-weight: 700; }
.brand .dot { width: 12px; height: 12px; border-radius: 50%; background: linear-gradient(135deg, #ff5c8a, #ff8fb1); box-shadow: 0 0 10px rgba(255, 92, 138, 0.6); }
.nav-links { display: flex; align-items: center; gap: 14px; flex-wrap: wrap; }
.nav-links a { padding: 8px 14px; border-radius: 10px; color: var(--text); font-weight: 500; text-decoration: none; transition: background 0.2s ease; }
.nav-links a:hover { background: rgba(255,92,138,0.1); }
.cart-link { position: relative; display: inline-flex; align-items: center; gap: 6px; }
.cart-badge { display:inline-flex; align-items:center; justify-content:center; min-width:20px; height:20px; padding:2px 6px; border-radius:999px; background:#16804a; color:#fff; font-size:12px; font-weight:700; }
.page-grid { margin-top: 32px; display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 20px; }
.status-card {
  background: var(--card);
  border-radius: 24px;
  padding: 32px;
  box-shadow: var(--shadow);
  text-align: center;
}
.status-card.success { border: 1px solid rgba(34, 197, 94, 0.3); }
.status-icon {
  width: 80px;
  height: 80px;
  border-radius: 50%;
  background: var(--success);
  color: #fff;
  margin: 0 auto 20px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 36px;
}
.detail-panel {
  background: var(--card);
  border-radius: 20px;
  padding: 24px;
  border: var(--border);
  box-shadow: 0 12px 35px rgba(17,24,39,0.08);
}
.detail-panel h3 { margin-top: 0; }
.detail-list { list-style: none; padding: 0; margin: 16px 0; display: grid; gap: 12px; }
.detail-list li {
  background: rgba(255, 92, 138, 0.08);
  border-radius: 12px;
  padding: 12px 14px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}
.detail-list li span:first-child { font-weight: 600; color: #c026d3; }
.detail-list li span:last-child { color: var(--muted); }
.actions { display: flex; flex-wrap: wrap; gap: 10px; justify-content: center; margin-top: 24px; }
.btn {
  border-radius: 12px;
  border: none;
  padding: 12px 18px;
  font-weight: 600;
  cursor: pointer;
  text-decoration: none;
}
.btn.primary { background: linear-gradient(135deg, #ff91b9, #f04dd0); color: #fff; }
.btn.secondary { background: #fff; border: 1px solid rgba(17,24,39,0.2); color: var(--text); }
.note { font-size: 14px; color: var(--muted); margin-top: 12px; }
@media (max-width: 640px) {
  .nav { flex-direction: column; gap: 8px; }
}
  </style>
</head>
<body>
  <div class="container">
    <nav class="nav">
      <div class="brand">
        <span class="dot"></span>
        ZozoMart
      </div>
      <div class="nav-links">
        <a href="/shopping">Home</a>
        <a href="/favorites">Favorites</a>
        <a href="/cart" class="cart-link">Cart <% if (cartCount > 0) { %><span class="cart-badge"><%= cartCount %></span><% } %></a>
      </div>
    </nav>

    <div class="page-grid">
      <div class="status-card success">
        <div class="status-icon">&#10003;</div>
        <h2>Order placed successfully</h2>
        <p class="muted">Your groceries are staged with care. Order #<%= orderId %> has been confirmed.</p>
        <p class="note">We will notify you once the rider picks up the bag. Estimated delivery updates will appear in your dashboard.</p>
        <div class="actions">
          <a class="btn primary" href="/orders">View my orders</a>
          <a class="btn secondary" href="/shopping">Continue shopping</a>
          <a class="btn secondary" href="/orders/<%= orderId %>/invoice" target="_blank">Print invoice (PDF)</a>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
