<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ZozoMart | Supermarket</title>
  <style>
@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap');

:root {
    --bg: #ffffff;
    --panel: #f7f8fc;
    --card: #ffffff;
    --accent: #ff5c8a;
    --accent-2: #ff8fb1;
    --text: #1f2430;
    --muted: #6b7280;
    --border: #e5e7eb;
    --shadow: 0 12px 30px rgba(17, 24, 39, 0.08);
    --radius: 16px;
}

* { box-sizing: border-box; }

body {
    margin: 0;
    font-family: 'Poppins', sans-serif;
    background: radial-gradient(circle at 20% 20%, rgba(255, 92, 138, 0.08), transparent 35%), radial-gradient(circle at 80% 10%, rgba(255, 143, 177, 0.12), transparent 32%), linear-gradient(135deg, #ffffff, #fdf3f7 45%, #ffffff);
    color: var(--text);
    min-height: 100vh;
}

a { color: var(--accent); text-decoration: none; }
a:hover { color: #ff3d77; }

.shell {
    max-width: 1200px;
    margin: 0 auto;
    padding: 24px;
}

.nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    background: #ffffff;
    backdrop-filter: blur(8px);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 12px 18px;
    box-shadow: var(--shadow);
    position: sticky;
    top: 18px;
    z-index: 10;
}

.brand {
    display: flex;
    align-items: center;
    gap: 10px;
    font-weight: 700;
    font-size: 18px;
    letter-spacing: .5px;
    color: var(--text);
}

.brand .dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--accent), var(--accent-2));
    box-shadow: 0 0 10px rgba(255, 92, 138, 0.6);
}

.nav-links {
    display: flex;
    align-items: center;
    gap: 14px;
    flex-wrap: wrap;
}

.nav-links a {
    padding: 8px 14px;
    border-radius: 10px;
    color: var(--text);
    font-weight: 500;
    transition: background 0.2s, color 0.2s, transform 0.2s;
}

.nav-links a:hover {
    background: rgba(255, 92, 138, 0.12);
    color: var(--text);
    transform: translateY(-1px);
}
.nav-links .pill:hover {
    background: linear-gradient(135deg, var(--accent), var(--accent-2));
    color: #ffffff !important;
    box-shadow: 0 12px 32px rgba(255, 92, 138, 0.35);
}

.pill {
    background: linear-gradient(135deg, var(--accent), var(--accent-2));
    color: #ffffff !important;
    box-shadow: 0 10px 30px rgba(255, 92, 138, 0.35);
}

.hero {
    display: grid;
    grid-template-columns: 1fr;
    gap: 28px;
    margin: 28px 0;
    align-items: center;
}

.hero-card {
    background: #ffffff;
    border: 1px solid var(--border);
    border-radius: calc(var(--radius) + 6px);
    padding: 28px;
    box-shadow: var(--shadow);
}

.eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 8px 12px;
    background: rgba(255, 92, 138, 0.14);
    border-radius: 999px;
    color: var(--text);
    font-size: 13px;
    letter-spacing: .4px;
}

.hero h1 {
    margin: 12px 0 10px;
    font-size: 36px;
    line-height: 1.2;
}

.hero p {
    color: var(--muted);
    margin: 0 0 18px;
}

.cta-row {
    display: flex;
    gap: 12px;
    flex-wrap: wrap;
    margin-top: 12px;
}

.cta {
    padding: 12px 16px;
    border: none;
    border-radius: 12px;
    font-weight: 600;
    cursor: pointer;
    transition: transform 0.2s, box-shadow 0.2s;
    color: #ffffff;
    background: linear-gradient(135deg, var(--accent), var(--accent-2));
    box-shadow: 0 10px 30px rgba(255, 92, 138, 0.35);
}

.cta.secondary {
    background: transparent;
    color: var(--text);
    border: 1px solid var(--border);
    box-shadow: none;
}

.cta:hover { transform: translateY(-1px); }

.stat-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
    gap: 16px;
    margin-top: 10px;
}

.stat {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 14px 16px;
    box-shadow: var(--shadow);
}

.stat label { color: var(--muted); font-size: 13px; }
.stat strong { display: block; margin-top: 8px; font-size: 22px; }

.section-head {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    margin-bottom: 14px;
}

.section-head h2 {
    margin: 0;
    font-size: 22px;
}

.products {
    margin-top: 18px;
}

.grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 16px;
}

.card {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 16px;
    display: flex;
    flex-direction: column;
    gap: 12px;
    box-shadow: var(--shadow);
    transition: transform 0.15s, border-color 0.15s, box-shadow 0.15s;
}

.card:hover {
    transform: translateY(-2px);
    border-color: rgba(255, 92, 138, 0.4);
    box-shadow: 0 16px 36px rgba(255, 92, 138, 0.15);
}

.card img {
    width: 100%;
    height: 160px;
    object-fit: cover;
    border-radius: 12px;
    background: #f3f4f6;
}

.card h3 {
    margin: 0;
    font-size: 17px;
}

.price {
    color: var(--accent);
    font-weight: 700;
    font-size: 18px;
}

.muted { color: var(--muted); font-size: 13px; }

.pill-badge {
    display: inline-flex;
    padding: 6px 10px;
    border-radius: 999px;
    background: rgba(255, 92, 138, 0.1);
    color: var(--accent);
    font-size: 12px;
}

.table-shell {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 16px;
    box-shadow: var(--shadow);
    overflow: hidden;
}

table {
    width: 100%;
    border-collapse: collapse;
    color: var(--text);
}

th, td {
    padding: 12px 10px;
    border-bottom: 1px solid var(--border);
    text-align: left;
}

th { color: var(--muted); font-weight: 600; font-size: 13px; }

.btn-link {
    color: var(--accent);
    font-weight: 600;
}

.inline-form { display: inline; }

.card-actions {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
}

.pill-action {
    padding: 10px 12px;
    border-radius: 10px;
    border: 1px solid var(--border);
    background: rgba(255, 92, 138, 0.08);
    color: var(--text);
    font-weight: 600;
    cursor: pointer;
}

.danger {
    background: rgba(231, 76, 60, 0.12);
    border-color: rgba(231, 76, 60, 0.3);
    color: #d14343;
}

.tag {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 8px 10px;
    background: rgba(255, 92, 138, 0.08);
    border-radius: 10px;
    color: var(--muted);
}

.spacer { height: 16px; }

.select {
    padding: 10px 12px;
    border-radius: 10px;
    border: 1px solid var(--border);
    background: #ffffff;
    color: var(--text);
    font-weight: 600;
}

.cart-link {
    position: relative;
    display: inline-flex;
    align-items: center;
    gap: 6px;
}

.cart-badge {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 20px;
    height: 20px;
    padding: 2px 6px;
    border-radius: 999px;
    background: #16804a;
    color: #fff;
    font-size: 12px;
    font-weight: 700;
}

@media (max-width: 900px) {
    .hero { grid-template-columns: 1fr; }
    .nav { position: static; }
}

.single-hero {
    grid-template-columns: 1fr;
}

</style>
</head>
<body>
  <div class="shell">
    <%- include('partials/nav') %>

    <section class="hero single-hero">
      <div class="hero-card">
        <div class="eyebrow">Everything fresh • Delivered fast</div>
        <h1>Groceries, snacks, and daily essentials in one vibrant marketplace.</h1>
        <p>Browse our shelves, discover new favorites, and keep your cart ready. Log in when you're ready to buy.</p>
        <div class="cta-row">
          <% if (user) { %>
            <a class="cta" href="/shopping">Start shopping</a>
            <a class="cta secondary" href="/cart">View cart</a>
          <% } else { %>
            <a class="cta" href="/login">Sign in to checkout</a>
            <a class="cta secondary" href="/register">Create a free account</a>
          <% } %>
        </div>
        <div class="stat-grid">
          <div class="stat">
            <label>Premium picks</label>
            <strong>Hand-picked produce</strong>
          </div>
          <div class="stat">
            <label>Fast checkout</label>
            <strong>Save your favorites</strong>
          </div>
          <div class="stat">
            <label>Secure</label>
            <strong>Encrypted sessions</strong>
          </div>
        </div>
      </div>
    </section>

    <section class="products" id="products">
      <div class="section-head">
        <h2>Featured products</h2>
        <% if (!user) { %><span class="muted">Log in to add items to cart</span><% } %>
      </div>
      <div class="grid">
        <% (products || []).forEach(function(p){ %>
          <div class="card">
            <img src="/images/<%= p.image %>" alt="<%= p.productName %>">
            <div class="pill-badge">In stock: <%= p.stock %></div>
            <h3><%= p.productName %></h3>
            <div class="price">$ <%= Number(p.price).toFixed(2) %></div>
            <div class="card-actions">
              <% if (user) { %>
                <form action="/add-to-cart/<%= p.id %>" method="POST">
                  <input type="hidden" name="quantity" value="1">
                  <button class="pill-action" type="submit">Add to cart</button>
                </form>
              <% } else { %>
                <a class="pill-action" href="/login">Login to buy</a>
              <% } %>
              <% if (user && user.role === 'admin') { %>
                <a class="pill-action" href="/editProduct/<%= p.id %>">Edit</a>
              <% } %>
            </div>
          </div>
        <% }) %>
      </div>
    </section>
  </div>
</body>
</html>







